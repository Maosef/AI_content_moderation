# FastAPI and web server
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3

# LLM Guard dependencies
numpy<2
llm-guard==0.3.12

# TLDD dependencies
transformers>=4.30.0
torch>=2.0.0
openai>=1.0.0
datasets>=2.14.0

# Optional: Golden retriever for RAG (may need to be installed separately)
# golden
